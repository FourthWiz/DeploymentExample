{
    "name": "Deployment app",
    "description": "A Python app with Heroku CI setup",
    "scripts": {
      "test": "mlflow run . --no-conda && pytest"
    },
    "environments": {
      "test": {
        "scripts": {
          "test": "mlflow run . --no-conda && pytest"
        }
      }
    }
  }